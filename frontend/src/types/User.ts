export interface User {
  email: string;
  first_name: string;
  id: number;
  is_email_verified: boolean;
  is_staff: boolean;
  last_name: string;
  // phone_number: string;
  // delivery_address: number;
}